<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Showroom</title>
    <script src="../../javascripts/logout.js" defer></script>
</head>
<body>

<%-include('navbar')%>

<div class="container">
    <div class="text-center">
        <h1>Showroom</h1>

        <% if (cars.length > 0) { %>
            <div class="row">
                <% cars.forEach(function(car) { %>
                    <div class="col-md-3 my-2 mx-4">
                        <div class="card mb-5" onclick="location.href='<%= car.link %>';">
                            <img src="<%= car.image %>" class="card-image-top" alt="Immagine auto">
                            <div class="card-body">
                                <h5 class="card-title"><%= car.marca %> <%= car.modello %></h5>
                                <p class="card-text"><%= car.prezzo %>€</p>
                                <a href="/cars/favourites/add/<%= car.id %>" class="btn btn-primary">Aggiungi ai Preferiti</a>
                                <a href="/cars/cart/add/<%= car.id %>" class="btn btn-warning">Aggiungi al carrello</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p>Nessuna auto disponibile.</p>
        <% } %>
    </div>
</div>
</body>
</html>
